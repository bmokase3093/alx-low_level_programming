#include<stdio.h>
/**
 * main - print the name of this program
 * @argc: count to count number of arguments passed in cmd
 * @argv: array of pointer to strings
 *
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	printf("%s\n",argv[0]);
	return (0);
}
